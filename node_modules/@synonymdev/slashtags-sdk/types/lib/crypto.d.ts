/**
 * Generates a seed from the slashtags Namespace, name and primaryKey.
 *
 * @param {Uint8Array} pk
 * @param {string} [name]
 * @returns
 */
export function generateSeed(pk: Uint8Array, name?: string | undefined): Buffer;
/**
 * Generates an blake2b hash of a buffer or string.
 *
 * @param {Uint8Array | string} input
 */
export function hash(input: Uint8Array | string): Buffer;
//# sourceMappingURL=crypto.d.ts.map